{
    "name": "blap64",
    "version": "1.0.0",
    "description": "",
    "author": "",
    "license": "ISC",
    "dependencies": {
        "gh-pages": "^6.3.0",
        "lit": "^3.2.1",
        "p5": "^2.0.3",
        "p5.sound": "^0.2.0",
        "parcel": "^2.15.4"
    },
    "scripts": {
        "dev": "parcel index.html --port 4321",
        "predeploy": "rm -rf dist && parcel build index.html --public-url https://blap64.xyz && cp CNAME ./dist && cp public/og-image.webp ./dist && cp -r links about interactive photo dist/",
        "deploy": "gh-pages -d dist"
    },
    "devDependencies": {
        "@parcel/packager-raw-url": "^2.13.3",
        "@parcel/transformer-webmanifest": "^2.13.3"
    }
}
